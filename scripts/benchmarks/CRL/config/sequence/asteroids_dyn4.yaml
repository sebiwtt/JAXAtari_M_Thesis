# @package _global_
# Ordered single-mod tasks; index 0 must be the base task (no mods). Mod keys are
# from src/jaxatari/games/mods/asteroids_mods.py. At most one mod per task.
ENV_ID: asteroids
SEQUENCE: dyn4
TASK_MODS:
  - []
  - ["faster_asteroids"] # make them even faster
  - ["faster_ship"] # make it even faster
  - ["randomize_asteroid_spawn"] # instead also spawn more (like a lot)
  - ["ship_inertia"] # tweak this even more / or swap with dont_shoot

# --- More Options ---
# slower_asteroids
# slower_ship
# dont_shoot
# instant_turn
