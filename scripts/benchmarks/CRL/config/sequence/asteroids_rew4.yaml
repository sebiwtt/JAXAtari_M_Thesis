# @package _global_
# Ordered single-mod tasks; index 0 must be the base task (no mods). Mod keys are
# from src/jaxatari/games/mods/asteroids_mods.py. At most one mod per task.
ENV_ID: asteroids
SEQUENCE: rew4
TASK_MODS:
  - []
  - ["life_loss_penalty"]
  - ["large_asteroid_only"]
  - ["small_asteroid_only"]
  - ["survival_reward"]

# --- More Options ---
# every_k_kills
# wave_clear_bonus
# flatten_asteroid_values  # NOTE: no-op under sign-clipped training reward (base
#                          # 20/50/100 all clip to +1); use only with clipping off.
