# @package _global_
# Ordered single-mod tasks; index 0 must be the base task (no mods). Mod keys are
# from src/jaxatari/games/mods/breakout_mods.py. At most one mod per task.
ENV_ID: breakout
SEQUENCE: rew4
TASK_MODS:
  - []
  - ["ball_loss_penalty"]
  - ["survival_reward"]
  - ["every_k_contacts"]
  - ["bottom_row_first"]

# --- More Options ---
# flatten_row_values  
# top_row_only -> yields 0 reward -> useless