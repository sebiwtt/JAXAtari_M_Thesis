# @package _global_
# Ordered single-mod tasks; index 0 must be the base task (no mods). Mod keys are
# from src/jaxatari/games/mods/breakout_mods.py. At most one mod per task.
ENV_ID: breakout
SEQUENCE: dyn4
TASK_MODS:
  - []
  - ["faster_ball"] # make this even faster
  - ["faster_paddle"] # make this even faster
  - ["random_serve"] # increase randomness some more or angle or something
  - ["ball_drift"] # add even more drift (but keep it playable)

# --- More Options ---
# slower_ball
# slower_paddle